#include <string>
#include <vector>
#include "QueryValidator.h"

QueryValidator::QueryValidator(Query query) {
	query = query;
}

bool QueryValidator::validate() {
	bool status = true;
	return status;
}

